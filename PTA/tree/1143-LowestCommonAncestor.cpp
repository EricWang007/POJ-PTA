#include<bits/stdc++.h>
using namespace std;

int pre[10005];

int main() {
    int M, N;
    scanf("%d%d", &M, &N);
    for (int i = 0; i < N; i++) 
        scanf("%d", &pre[i]);
    for (int i = 0; i < M; i++) {
        int k1, k2;
        scanf("%d%d", &k1, &k2);
        
    }
    return 0;
}